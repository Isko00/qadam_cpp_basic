#include <iostream>

using namespace std;

int main() {
	bool isRed = 0; isGreen = 0; isBlue = 0;

	if (isRed) {
		cout << "red";
	} else if (isGreen) {
		cout << "green";
	} else if (isBlue) {
		cout << "blue";
	} else {
		cout << "do not know";
	}

	return 0;
}